\documentclass{article}
\usepackage{tikz}
\usetikzlibrary{shapes,arrows.meta, positioning}

\begin{document}

% Define block styles
\tikzstyle{startstop} = [rectangle, rounded corners, minimum width=3cm, minimum height=1cm,text centered, draw=black, fill=red!30]
\tikzstyle{io} = [trapezium, trapezium left angle=70, trapezium right angle=110, minimum width=3cm, minimum height=1cm, text centered, draw=black, fill=blue!30]
\tikzstyle{decision} = [diamond, minimum width=3cm, minimum height=1cm, text centered, draw=black, fill=green!30]
\tikzstyle{arrow} = [thick,->,>=stealth]

\begin{tikzpicture}[node distance=2cm, every node/.style={scale=0.8}]
    % Nodes
    \node (start) [startstop] {Start};
    \node (input) [io, below of=start] {Input Software Name};
    \node (process1) [io, below of=input] {Display Options};
    \node (login) [io, below of=process1, yshift=-0.5cm] {Login};
    \node (register) [io, left of=login, xshift=-3cm] {Register};
    \node (exit) [io, right of=login, xshift=3cm] {Exit Program};
    % Arrows
    \draw [arrow] (start) -- (input);
    \draw [arrow] (input) -- (process1);
    \draw [arrow] (process1) -- (login);
    \draw [arrow] (process1) -- (register);
    \draw [arrow] (process1) -- (exit);
\end{tikzpicture}

\end{document}
